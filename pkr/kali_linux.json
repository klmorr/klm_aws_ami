{
    "builders": [
        {
            "region": "{{user `region`}}",
            "ami_name": "kali-{{isotime \"02Jan2006\"}}",
            "instance_type": "{{user `instance_type`}}",
            "source_ami": "{{user `source_ami`}}",
            "force_deregister": true,
            "source_ami_filter": {
                "most_recent": true,
                "owners": "{{user `ami_owners`}}"
            },
            "ssh_username": "kali",
            "ssh_read_write_timeout": "10m",
            "type": "amazon-ebs",
            "tags": {
                "Name": "{{user `name`}}",
                "description": "{{user `description`}}",
                "builder": "{{user `builder`}}"
            }
        }
    ],
    "provisioners": [
        {
            "type": "shell",
            "environment_vars": "DEBIAN_FRONTEND=noninteractive",
            "execute_command": "sudo -S env {{ .Vars }} {{ .Path }}",
            "inline": [
                "apt update && upgrade && apt dist-upgrade -y",
                "apt-get install python3 software-properties-common jq curl unzip ansible -y"
            ]
        }
    ],
    "post-processors": [
        {
            "type": "manifest",
            "output": "kali_linux_manifest.json",
            "strip_path": true
        }
    ]
}